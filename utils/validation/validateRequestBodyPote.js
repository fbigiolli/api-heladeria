exports.validateRequestBodyPote = function validateRequestBodyPote(body) {
    // swagger verifica que cumpla estructuralmente todo, si llega aca ya tiene peso correcto x enum y gustos.
    // hay que validar que los gustos existan en la heladeria

    return true;
}